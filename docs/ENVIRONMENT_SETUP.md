
# Environment Setup

1. Copy `frontend/.env.example` to `frontend/.env.local` and populate values.
2. Copy `backend/.env.example` to `backend/.env` and populate values.
3. Ensure Supabase local or remote instance is running.
4. Obtain a Google Gemini API Key from Google AI Studio.
