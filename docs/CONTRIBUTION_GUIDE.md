# Contribution Guide

1. Create a feature branch from `main`.
2. Follow strict TypeScript typing in the frontend.
3. Follow strict Pydantic typing in the backend.
4. All UI components must use Tailwind CSS tokens defined in `globals.css`. No hardcoded colors.
5. Submit PR for review.
