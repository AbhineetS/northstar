# Development Guide

## Frontend
1. Navigate to `/frontend`
2. Run `npm install`
3. Run `npm run dev`
## Backend
1. Navigate to `/backend`
2. Activate virtual environment: `source venv/bin/activate`
3. Install dependencies: `pip install -r requirements.txt` (Note: requirements generated separately)
4. Run server: `uvicorn main:app --reload`
